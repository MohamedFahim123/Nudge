import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Nudge | Home",
  description: "Home Page",
};

const HomePage = () => {
  return <>Home Page</>;
};

export default HomePage;
